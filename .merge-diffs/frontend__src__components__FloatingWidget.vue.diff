diff --git a/frontend/src/components/FloatingWidget.vue b/frontend/src/components/FloatingWidget.vue
index f6be19b..c3386be 100644
--- a/frontend/src/components/FloatingWidget.vue
+++ b/frontend/src/components/FloatingWidget.vue
@@ -14,14 +14,14 @@
         role="progressbar"
         :aria-valuenow="todayFaith"
         aria-valuemin="0"
-        aria-valuemax="100"
-        :aria-label="`今日信仰进度 ${todayFaith}/100`"
+        aria-valuemax="1000"
+        :aria-label="`今日信仰进度 ${todayFaith}/1000`"
       >
         <div class="progress-label">今日</div>
         <div class="progress-bar">
           <div class="progress-fill" :style="{ width: todayPercent + '%' }"></div>
         </div>
-        <div class="progress-text">{{ todayFaith }}/100</div>
+        <div class="progress-text">{{ todayFaith }}/1000</div>
       </div>
       <div v-if="error" class="error-message" role="alert">{{ error }}</div>
       <div class="quick-actions" role="group" aria-label="快速操作">
@@ -50,7 +50,7 @@ const todayFaith = computed(() => {
   return (t.survival_faith || 0) + (t.progress_faith || 0) + (t.discipline_faith || 0);
 });
 
-const todayPercent = computed(() => Math.min((todayFaith.value / 100) * 100, 100));
+const todayPercent = computed(() => Math.min((todayFaith.value / 1000) * 100, 100));
 
 async function loadStatus() {
   currentController?.abort();
@@ -80,6 +80,9 @@ async function quickCheckIn(workHours: number, studyHours: number) {
       userId: 'default_user',
       workMinutes: workHours * 60,
       studyMinutes: studyHours * 60,
+      breakCount: 0,
+      leaveRecord: 0,
+      closeRecord: 1,
       signal: currentController.signal,
     });
   } catch (e) {
@@ -275,4 +278,4 @@ onUnmounted(() => {
   opacity: 0.5;
   cursor: not-allowed;
 }
-</style>
\ No newline at end of file
+</style>
